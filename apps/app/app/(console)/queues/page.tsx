import { Placeholder } from "@/components/placeholder"

export default function QueuesPage() {
  return (
    <Placeholder surface="console" milestone="M2">
      Mine, my teams, all open, unassigned — with filters and sort in the URL,
      so a view is a link a colleague in the same workspace can open. Buttons
      render from the server&apos;s <code>availableActions</code>, never from a
      re-derived role check.
    </Placeholder>
  )
}
