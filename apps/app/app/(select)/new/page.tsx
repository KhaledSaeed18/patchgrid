import { Placeholder } from "@/components/placeholder"

export default function NewWorkspacePage() {
  return (
    <Placeholder surface="workspace picker" milestone="M1">
      Name and slug, with live availability. Provisioning is one transaction —
      organization, owner membership, default teams, the category tree, eight
      SLA policies and starter articles — and is idempotent, so a retry never
      double-seeds.
    </Placeholder>
  )
}
